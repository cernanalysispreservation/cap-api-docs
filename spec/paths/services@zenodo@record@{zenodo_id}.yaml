parameters:
  - name: zenodo_id
    in: path
    description: Zenodo ID of a zenodo record
    schema:
      type: string
    required: true
get:
  tags:
    - Services
  summary: Get a Zenodo record
  operationId: getZenodoRecord
  responses:
    '200':
      description: OK
      headers:
        X-Rate-Limit:
          $ref: '#/components/headers/RateLimit'
        X-Expires-After:
          $ref: '#/components/headers/ExpiresAfter'
      content:
        application/json:
          schema:
            type: object
    '401':
      $ref: '#/components/responses/NotAuthorized'